Welcome to the AI Powered African Wildlife Detection Repository!

How to run the app:

1. `cd frontend`
2. `npm install`
3. `npm start` to run a dev instance; `npm run build` and then `serve -s build` to deploy a local instance
